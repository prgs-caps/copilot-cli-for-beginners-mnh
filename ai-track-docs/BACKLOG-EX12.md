# Backlog — Ex12 Grooming

Generated by Copilot based on findings in `samples/book-app-project/`.

---

## Item 1 — Add GitHub Actions CI workflow for book-app-project

**Observation:** No CI workflow runs tests for the Python sub-project. Only course/traffic updater workflows exist in `.github/workflows/`.

**Code link:** [`samples/book-app-project/pyproject.toml`](../samples/book-app-project/pyproject.toml)

**Acceptance Criteria:**
- A `.github/workflows/book-app-tests.yml` workflow exists
- It triggers on push and pull_request to any `crawl/**` branch
- It runs `pip install pytest>=9.0,<10` and `pytest -v` in `samples/book-app-project/`
- The workflow exits non-zero if any test fails

---

## Item 2 — Extend structured logging to mark_as_read and remove_book

**Observation:** Only `add_book` emits a structured log (Ex9). The other two mutating methods — `mark_as_read` and `remove_book` — are uninstrumented, making it harder to trace failures.

**Code link:** [`samples/book-app-project/books.py` — `mark_as_read` and `remove_book`](../samples/book-app-project/books.py)

**Acceptance Criteria:**
- `mark_as_read` emits a `DEBUG` log with fields `op`, `status` (`ok` or `not_found`), `title`, `elapsed_ms`
- `remove_book` emits the same fields
- No new test failures introduced
- `ai-track-docs/logging.md` updated to reflect new instrumented paths

---

## Item 3 — Add coverage reporting to the test suite

**Observation:** Tests pass but there is no coverage measurement. It is unknown which branches in `load_books`, `find_book_by_title`, and `find_by_author` are exercised.

**Code link:** [`samples/book-app-project/tests/test_books.py`](../samples/book-app-project/tests/test_books.py)

**Acceptance Criteria:**
- `pytest-cov` added to `pyproject.toml` dependencies
- `pytest --cov=books --cov-report=term-missing` runs without error
- Coverage report shows ≥80% for `books.py`
- `ai-track-docs/build-test.md` updated with the coverage command

---

## Item 4 — Validate that data.json is not corrupted on write

**Observation:** `load_books` handles `json.JSONDecodeError` gracefully, but `save_books` has no error handling — a partial write (e.g. disk full) would silently corrupt the file.

**Code link:** [`samples/book-app-project/books.py` — `save_books`](../samples/book-app-project/books.py)

**Acceptance Criteria:**
- `save_books` writes to a temp file first, then atomically replaces `DATA_FILE`
- OR `save_books` catches `OSError` and raises a clear exception with context
- A test simulates a write failure and asserts the original data is preserved
- Existing 9 tests continue to pass

---

## Item 5 — Add find_by_author coverage and a missing-author negative test

**Observation:** `find_by_author` exists and has a docstring but has no test coverage at all — it is not exercised by any of the 9 current tests.

**Code link:** [`samples/book-app-project/books.py` — `find_by_author`](../samples/book-app-project/books.py)

**Acceptance Criteria:**
- A test asserts `find_by_author` returns the correct books for a known author
- A negative test asserts `find_by_author` returns `[]` for an unknown author
- A case-insensitivity test asserts `find_by_author("frank herbert")` matches `"Frank Herbert"`
- All tests pass with `pytest -v`
